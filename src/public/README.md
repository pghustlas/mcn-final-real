# Public Assets Folder

이 폴더(`public`)는 이미지, 폰트 등 정적 파일을 저장하는 곳입니다.

## 사용 방법
1. 이 폴더에 이미지 파일(예: `hero-bg.png`)을 넣어주세요.
2. 코드에서 다음과 같이 사용할 수 있습니다:

```tsx
// Hero.tsx 등에서
<img src="/hero-bg.png" alt="Hero Background" />
```

주의: `import` 문을 쓰지 않고 문자열 경로로 바로 사용할 수 있습니다.
